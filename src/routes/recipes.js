import express from "express";
import bodyParser from "body-parser";
import RecipesModel from "../models/Recipes";

const recipesRoutes = express.Router();

var jsonParser = bodyParser.json();

// configurer le recipesRoutes.get

// configurer le recipesRoutes.get avec id

// configurer le recipesRoutes.post

// configurer le recipesRoutes.patch

// configurer le recipesRoutes.delete

export default recipesRoutes;
